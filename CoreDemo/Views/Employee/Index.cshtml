@model CoreDemo.Controllers.HomePageViewModel
@{
    ViewBag.Title = "Employee List";
}
<h1>Employee List</h1>
<div>这个消息来自 Employee 控制器下的 Index 的视图文件 Index.cshtml </div>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    @*<thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Employees[0].Id)</th>
                <th>@Html.DisplayNameFor(model => model.Employees[0].Code)</th>
                <th>@Html.DisplayNameFor(model => model.Employees[0].Name)</th>
                <th>@Html.DisplayNameFor(model => model.Employees[0].Remark)</th>
                <th></th>
            </tr>
        </thead>*@
    <thead>
        <tr>
            <th>Id</th>
            <th>Code</th>
            <th>Name</th>
            <th>Remark</th>
            <th>Opera</th>
        </tr>
    </thead>
    @foreach (var item in Model.Employees)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Code</td>
            <td>@item.Name</td>
            <td>@item.Remark</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Detail" asp-route-id="@item.Id">Detail</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
    }
</table>

